language: python
sudo: false
python: 
    - 3.5.4
# cache: apt
# addons:
#     apt:
#         pacakge
# cache:
#     directories:
#         - $HOME/anaconda3
branches:
    only:
        - master
before_install:
    - 
install:
    - bash ./utilities/travis_setup.sh
    - export PATH="$HOME/anaconda3/bin:$PATH"
    - hash -r 
    - source activate asteroid
script:
    - pytest --cov-config .coveragerc --cov=dynamics --cov=point_cloud --cov=eom_comparison --cov=landing  tests/test_raycaster.py
after_success:
    - coveralls

